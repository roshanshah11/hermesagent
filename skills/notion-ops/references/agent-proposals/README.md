# Agent-proposed learnings (Plan 09 review queue)

Files HERMES wrote itself mid-task on 2026-06-10 while solving the write-test (it edited its own
skill via file tools). Curation verdict: the ["value","value"] pitfall was promoted into SKILL.md
and write-recipes.md; the whole-["properties"]-object set pattern was REJECTED (clobbers concurrent
property edits — the per-property recipe is the standard); the python snippets here drive notion_v3
via code-exec rather than MCP tools, so they're kept as reference, not doctrine.
